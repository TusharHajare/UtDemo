server.port=8099
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=root
spring.datasource.password=root
spring.h2.console.enabled=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

#1>>how we can add the configuration in the properties file -> if any need to add the data through
# - application.properties file --> below property

#welcome.message = welcome to the unit testing demo application

#2>> spring boot profiles in yml file
#

#server.port=8099
#spring.datasource.url = jdbc:mysql://localhost:3306/utemployeedb
#spring.datasource.username = root
#spring.datasource.password = Tushar@123
#spring.jpa.show-sql = true
#spring.jpa.hibernate.ddl-auto = update
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Update - alter the existing table with the new table name.

#spring.jpa.hibernate.ddl-auto = create
#CREATE - Drop the existing table and create a new table with the same name.


